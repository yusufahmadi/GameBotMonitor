<Window x:Class="GameBotMonitor.Views.CalibrationViewerOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preview Posisi Kalibrasi"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#55000000"
        Topmost="True"
        ShowInTaskbar="False"
        KeyDown="Window_KeyDown"
        MouseDown="Window_MouseDown">
    <Grid>
        <!-- Canvas untuk meletakkan titik pin, panah, dan label -->
        <Canvas x:Name="OverlayCanvas" ClipToBounds="True"/>

        <!-- Header Bar Info & Tombol Tutup -->
        <Border HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"
                Background="#EE181825" CornerRadius="10" Padding="16,10"
                BorderBrush="#89B4FA" BorderThickness="1.5">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock x:Name="TxtTitleHeader" Text="👁️ PREVIEW POSISI KALIBRASI" FontWeight="Bold" FontSize="14" Foreground="#89B4FA" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <TextBlock Text="• Klik di mana saja atau tekan [ESC] untuk menutup" FontSize="12" Foreground="#A6ADC8" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <Button Content="✖ Tutup" Width="70" Height="26" FontWeight="Bold" FontSize="11"
                        Background="#F38BA8" Foreground="#11111B" BorderThickness="0" Cursor="Hand" Click="BtnClose_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="6,2">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
